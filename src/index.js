const genders = require('./constants/genders')
const ethnics = require('./constants/ethnics')
const policyStatuses = require('./constants/policyStatuses')

module.exports = {
  genders, 
  ethnics, 
  policyStatuses 
}
